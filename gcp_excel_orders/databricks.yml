# Declarative Automation Bundle configuration, more info here: https://docs.databricks.com/en/dev-tools/bundles/settings.html
bundle:
  name: "gcp_excel_orders"

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  # The 'dev' target, for development purposes. This target is the default.
  dev:
    mode: development
    default: true
    workspace:
      host: https://dbc-04988f51-57ce.cloud.databricks.com

  # The 'prod' target, used for production deployment.
  prod:
    mode: production
    workspace:
      host: https://dbc-04988f51-57ce.cloud.databricks.com
      # We always use /Users/minh-cuong.doan@teamwork.net for all resources to make sure we only have a single copy.
      root_path: /Users/minh-cuong.doan@teamwork.net/.bundle/${bundle.name}/${bundle.target}
    run_as:
      # This runs as minh-cuong.doan@teamwork.net in production. We could also use a service principal here,
      # see https://docs.databricks.com/dev-tools/bundles/permissions.html.
      user_name: minh-cuong.doan@teamwork.net
